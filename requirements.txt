# ScreenMonitorMCP v2.3+ - Core Dependencies (MCP-only mode)
# ==============================================================
# These are the minimal dependencies needed for MCP mode.
# For HTTP server mode with AI, install with: pip install -e ".[http]"
# For all features, install with: pip install -e ".[all]"

# Screen Capture & Image Processing
mss>=9.0.1
Pillow>=10.0.0

# System Monitoring
psutil>=5.9.0

# Configuration & Settings
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging
structlog>=23.1.0

# Database
aiosqlite>=0.19.0

# MCP Protocol
mcp>=1.0.0

# ==============================================================
# Optional HTTP Server Mode Dependencies
# ==============================================================
# Uncomment below if you need HTTP server with AI features:
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# openai>=1.3.0

# Or install with: pip install screenmonitormcp-v2[http]
